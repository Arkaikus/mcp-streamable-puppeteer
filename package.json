{
  "name": "mcp-streamable-js",
  "version": "0.1.0",
  "description": "Streamable HTTP MCP server for LM Studio (Bun)",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts"
  },
  "devDependencies": {
    "bun-types": "^1.3.10"
  },
  "dependencies": {
    "@hono/mcp": "^0.2.4",
    "@modelcontextprotocol/sdk": "^1.27.0",
    "hono": "^4.6.0",
    "puppeteer-core": "^24.0.0",
    "zod": "^3.24.0"
  }
}
